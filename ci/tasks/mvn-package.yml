platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 8-alpine

inputs:
  - name: employeemgmt-service

outputs:
  - name: build

run:
  path: employeemgmt-service/ci/tasks/mvn-package.sh
  